---
title: AI agent skills
description: "What package-published skills are and how they relate to the universal lpm skills manager."
---

**Agent skills** are markdown files that ship inside a package and tell an AI editor / agent how to use it correctly — usage examples, gotchas, common patterns, integration steps. When you install a skill-shipping package, LPM CLI extracts the skills into `.lpm/skills/<package>/` in your project and (optionally) auto-integrates them with your editor.

Skills are the LPM.dev Registry's native answer to the "the AI hallucinates this library's API" problem. The package author writes the skill at publish time; consumers get authoritative usage docs the agent can read directly.

## Status

Skills are currently a **lpm.dev-only** API behavior. Non-lpm.dev packages are skipped (no per-tarball skills declaration yet). Package authors publish skills via the lpm.dev UI; the LPM.dev Registry runs LLM security scans on shipped skill content at publish time, so consumed skills are attested.

## On the consumer side

Skills get installed automatically by [`lpm install`](/docs/packages/install) and [`lpm add`](/docs/packages/add) — you don't usually need to touch them by hand.

The universal [`lpm skills`](/docs/packages/skills) manager can also install standard `SKILL.md` directories from GitHub, local folders, and LPM.dev packages for Codex, Claude Code, and Cursor. It is separate from this legacy package-extraction layout:

```bash
lpm skills add @lpm.dev/owner.pkg --agent codex
lpm skills add vercel-labs/agent-skills
```

`lpm publish --check` remains the validation surface for skills authored and published inside an LPM.dev package. Skip package skill extraction per-invocation with `--no-skills` on `lpm install` or `lpm add`.

## On-disk layout

```text
my-project/
├── .lpm/
│   └── skills/
│       └── owner.pkg/
│           ├── usage.md
│           ├── gotchas.md
│           └── …
└── …
```

When skills install via `lpm install` or `lpm add`, LPM CLI auto-adds `.lpm/skills/` to your project's `.gitignore` (commitable by hand if you want the skills in source control). The standalone `lpm skills install` command does **not** touch `.gitignore` — add the entry yourself if you go through that path.

## Editor auto-integration

After a successful skill install, LPM CLI detects which AI editor your project is using (by looking for one of the marker files below) and appends a one-time reference block to its rules file pointing the agent at `.lpm/skills/`. The block is idempotent — re-installs don't duplicate it. Skip with `--no-editor-setup` on `lpm install` / `lpm add`.

| Editor | Detected via |
| --- | --- |
| Claude Code | `CLAUDE.md` at the project root |
| Cursor | `.cursor/rules/` directory |
| Cursor (legacy) | `.cursorrules` file |
| Windsurf | `.windsurfrules` file |
| GitHub Copilot | `.github/copilot-instructions.md` |
| Augment | `.augment/instructions.md` |
| Cline | `.clinerules` file |

If none of these markers exist, the auto-integration step is a no-op — your skills still install to `.lpm/skills/<package>/`, but no editor-side rules file is touched. Create the matching marker for your editor and re-run `lpm install` (or `lpm skills install <pkg>`) to opt in.

## `lpm skills`

Full command reference for universal skill discovery, installation, safety scanning, scopes, and agent targets lives on [`lpm skills`](/docs/packages/skills).

## See also

- [`lpm skills`](/docs/packages/skills) — command reference
- [`lpm install --no-skills`](/docs/packages/install) — skip skill installation per-invocation
- [`lpm add --no-skills`](/docs/packages/add) — same, for source delivery
- [MCP servers](/docs/reference/mcp-servers) — different mechanism for AI integration (tool calls vs. ambient docs)
